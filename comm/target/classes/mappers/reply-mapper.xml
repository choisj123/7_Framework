<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="reply-mapper">

	<!-- 댓글 목록 조회  -->
	<select id="selectReplyList" parameterType="_int" resultType="reply">
		SELECT REPLY_NO, REPLY_CONTENT, 
		TO_CHAR(CREATE_DT, 'YYYY"년" MM"월" DD"일" HH24:MI:SS') CREATE_DT,
		MEMBER_NO, BOARD_NO, PARENT_REPLY_NO
		FROM REPLY_S
		WHERE REPLY_ST = 'N'
		AND BOARD_NO = ${boardNo}
	</select>
	
	<!-- 댓글 등록 -->
	<insert id="insertReply">
		INSERT INTO REPLY_S VALUES(
			SEQ_REPLY_NO_S.NEXTVAL, ${replyContent}, DEFAULT, DEFAULT, 
			${memberNo}, ${boardNo}, ? 
		)
	
	</insert>
	
	<!-- 댓글 삭제 -->
	<update id="deleteReply">
		UPDATE REPLY_S 
		SET REPLY_ST = 'Y'
		WHERE REPLY_NO = ${replyNo}
	</update>

</mapper>
